@model IEnumerable<Taba_Digital.Models.Solicita>

@{
    
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.aldeia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.necessidades)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.aldeia.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.necessidades.Tipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Solicita">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Solicita">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Solicita">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
