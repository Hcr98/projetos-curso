@model IEnumerable<Taba_Digital.Models.Doacoes>

@{
    
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data_contribuicoes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.pessoa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.aldeia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.empresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data_contribuicoes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pessoa.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.aldeia.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.empresa.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Doacoes">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Doacoes">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Doacoes">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
